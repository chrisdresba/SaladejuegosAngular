import { NotimageDirective } from './notimage.directive';

describe('NotimageDirective', () => {
  it('should create an instance', () => {
    const directive = new NotimageDirective();
    expect(directive).toBeTruthy();
  });
});
